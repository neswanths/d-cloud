[package]
name    = "file_storage_integrity"
version = "0.1.0"
edition = "2021"

# LOCKED: all versions from workspace root — do not override locally
[lib]
name      = "file_storage_integrity"
crate-type = ["cdylib", "rlib"]

[dependencies]
hdi          = { workspace = true }
serde        = { workspace = true }
sha2         = { workspace = true }
hex          = { workspace = true }
ed25519-dalek = { workspace = true }
